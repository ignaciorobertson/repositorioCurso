export function NavRedes(props) {

    return (
        <header style={{ backgroundColor : '#255e17ff', padding: '10px', color: '#a3b9f5ff' }}>
            <a href={props.link} target="_blank">Escuchá lo último</a>
        </header>
    )
}